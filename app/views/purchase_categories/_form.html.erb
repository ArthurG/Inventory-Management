<%= form_for(@purchase_category) do |f| %>
  <% if @purchase_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_category.errors.count, "error") %> prohibited this purchase_category from being saved:</h2>

      <ul>
      <% @purchase_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <%= f.submit "Submit", class: "btn btn-primary" %>
  <%= link_to "Back", purchase_categories_path, class: "btn btn-default" %>
<% end %>
