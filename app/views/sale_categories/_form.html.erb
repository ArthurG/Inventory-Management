<%= form_for(@sale_category) do |f| %>
  <% if @sale_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale_category.errors.count, "error") %> prohibited this sale_category from being saved:</h2>

      <ul>
      <% @sale_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :fields %><br>
    <%= f.text_area :fields %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
